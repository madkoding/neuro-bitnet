# =============================================================================
# PRESET: MIROSTAT - Usa algoritmo Mirostat 2 para calidad consistente
# Mirostat ajusta automáticamente la "sorpresa" del texto
# Copia estas líneas a tu .env
# =============================================================================

# Mirostat 2 activado (ignora top_k, top_p, tfs, typical)
BITNET_MIROSTAT=2

# Tau: entropía objetivo
# 3.0 = muy coherente/predecible
# 5.0 = balance (default)
# 7.0 = más diverso/creativo
BITNET_MIROSTAT_TAU=5.0

# Eta: velocidad de ajuste (0.1 es buen default)
BITNET_MIROSTAT_ETA=0.1

# Temperature aún afecta la distribución inicial
BITNET_TEMPERATURE=0.8

# Estos se ignoran con Mirostat pero los dejamos por si desactivas
BITNET_TOP_K=40
BITNET_TOP_P=0.95
BITNET_MIN_P=0.05

# Penalizaciones siguen funcionando
BITNET_REPEAT_PENALTY=1.1
BITNET_REPEAT_LAST_N=64
BITNET_PRESENCE_PENALTY=0.0
BITNET_FREQUENCY_PENALTY=0.0
